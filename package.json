{
  "name": "sport-n-relax",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "format": "prettier --write .",
    "cli": "node .cli/index.js",
    "monorepo": "npx rimraf --glob ./**/node_modules && pnpm install"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "prettier": "^3.2.5",
    "rimraf": "3.0.2",
    "turbo": "^2.0.3"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  }
}
